### Test Provider API

### Variables
@baseUrl = http://localhost:3000

### Create a new provider
POST http://localhost:3000/api/v1/providers
Content-Type: application/json

{
    "name": "AWS",
    "state": "Enabled",
    "countryCode": "US",
    "attributes": {
        "regions": ["us-east-1", "us-west-2"],
        "tier": ["enterprise"]
    }
}

### Get a provider by ID
GET http://localhost:3000/api/v1/providers/{{providerId}}

### List all providers
GET http://localhost:3000/api/v1/providers

### Update a provider
PUT http://localhost:3000/api/v1/providers/{{providerId}}
Content-Type: application/json

{
    "name": "AWS Updated",
    "state": "Enabled",
    "countryCode": "US",
    "attributes": {
        "regions": ["us-east-1", "us-west-2", "eu-west-1"],
        "tier": ["enterprise", "premium"]
    }
}

### Delete a provider
DELETE http://localhost:3000/api/v1/providers/{{providerId}}

### Create a provider with invalid state (should fail)
POST http://localhost:3000/api/v1/providers
Content-Type: application/json

{
    "name": "Invalid Provider",
    "state": "InvalidState",
    "countryCode": "US"
}

### Create a provider with invalid country code (should fail)
POST http://localhost:3000/api/v1/providers
Content-Type: application/json

{
    "name": "Invalid Provider",
    "state": "Enabled",
    "countryCode": "USA"
}

### Create a provider with empty name (should fail)
POST http://localhost:3000/api/v1/providers
Content-Type: application/json

{
    "name": "",
    "state": "Enabled",
    "countryCode": "US"
}

### Create a provider with invalid attributes (should fail)
POST http://localhost:3000/api/v1/providers
Content-Type: application/json

{
    "name": "Invalid Provider",
    "state": "Enabled",
    "countryCode": "US",
    "attributes": {
        "empty_array": [],
        "empty_value": [""]
    }
}
