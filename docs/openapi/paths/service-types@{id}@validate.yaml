  parameters:
    - name: id
      in: path
      required: true
      schema:
        $ref: "../components/schemas/common.yaml#/properties.UUID"
  post:
    operationId: serviceTypesValidate
    summary: Validate service properties
    tags:
      - Services
    description: Validates service properties against the service type's property schema
    x-auth-permissions:
      - role: admin
        permission: all service types
      - role: participant
        permission: all service types
      - role: agent
        permission: all service types
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../components/schemas/service_types.yaml#/ValidatePropertiesReq"
    responses:
      "200":
        description: Validation results
        content:
          application/json:
            schema:
              $ref: "../components/schemas/service_types.yaml#/ValidatePropertiesRes"
      "400":
        description: Bad request (e.g., service type has no property schema)
        content:
          application/json:
            schema:
              $ref: "../components/schemas/common.yaml#/ErrorRes"
      "404":
        description: Service type not found
        content:
          application/json:
            schema:
              $ref: "../components/schemas/common.yaml#/ErrorRes"
#
# Service Group endpoints
#
