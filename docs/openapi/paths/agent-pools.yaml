get:
  operationId: agentPoolsList
  summary: List agent pools
  tags:
    - Agents
  description: Retrieves a paginated list of agent pools
  x-auth-permissions:
    - role: admin
      permission: all agent pools
    - role: agent
      permission: all agent pools (read-only)
  parameters:
    - name: page
      in: query
      schema:
        type: integer
        default: 1
    - name: pageSize
      in: query
      schema:
        type: integer
        default: 10
    - name: sort
      in: query
      schema:
        type: string
      description: "Sort field. Prefix with '+' for ascending or '-' for descending. Default is ascending. Supported fields: name, type, createdAt"
      example: "+name"
    - name: name
      in: query
      schema:
        type: array
        items:
          type: string
      description: Filter by name (case-insensitive substring match, can specify multiple values)
    - name: type
      in: query
      schema:
        type: array
        items:
          type: string
      description: Filter by pool type (can specify multiple values)
    - name: generatorType
      in: query
      schema:
        type: array
        items:
          type: string
          enum: [list]
      description: Filter by generator type (can specify multiple values)
  responses:
    "200":
      description: A paginated list of agent pools
      content:
        application/json:
          schema:
            allOf:
              - $ref: "../components/schemas/common.yaml#/PageRes"
              - type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "../components/schemas/agent_pools.yaml#/AgentPoolRes"
    "401":
      $ref: "../components/responses.yaml#/Unauthorized"
    "403":
      $ref: "../components/responses.yaml#/Forbidden"
post:
  operationId: agentPoolsCreate
  summary: Create an agent pool
  tags:
    - Agents
  description: Creates a new agent pool
  x-auth-permissions:
    - role: admin
      permission: always
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../components/schemas/agent_pools.yaml#/CreateAgentPoolReq"
  responses:
    "201":
      description: Agent pool created successfully
      content:
        application/json:
          schema:
            $ref: "../components/schemas/agent_pools.yaml#/AgentPoolRes"
    "400":
      $ref: "../components/responses.yaml#/BadRequest"
    "401":
      $ref: "../components/responses.yaml#/Unauthorized"
    "403":
      $ref: "../components/responses.yaml#/Forbidden"
